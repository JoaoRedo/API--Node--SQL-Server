1. Para criar o projeto basta criar a pasta do mesmo, abrí-la no code e no terminal digitar 'npx express-generator'.
2. Necessário instalar as dependências/bibliotecas:
  npm install
  npm install mssql